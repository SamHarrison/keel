# reqlib — shared kernels for the req CLI. Three pure modules everything
# agrees on (refs = the one reference grammar; hashing = what "changed"
# means; schemas = what "well-formed" means), per decisions K-16/K-17/K-21.
