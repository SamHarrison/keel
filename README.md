# <project name>

<!-- THE ROOT DOCUMENT. Humans enter the repo here; agents enter via
     CLAUDE.md (or AGENTS.md/codex.md equivalents), which points back here.
     This file is about YOUR product — the inception playbook fills it —
     and it carries the VISION: the top of the product spine lives here,
     not in a separate file. Budget: ≤200 lines, ≤20 [#TOKEN] anchors on
     load-bearing commitments only. -->

<one paragraph: what this product is, for whom, and why it matters.
Owner: <email>.>

## Vision

### Problem & stakes
<why this must exist; what stays broken otherwise>

### What this is — and is not
<solution class>  [#XXXXX]
Non-goals: <explicit exclusions>  [#XXXXX]

### Commitments
- <invariant promise the product will keep>  [#XXXXX]

## Status

<phase · latest baseline tag (git tag -l 'baseline/*') · roadmap: link your
GitHub Project — execution lives in issues, truth lives in docs/.>

## Getting started

```sh
make init     # once per clone: venv + deps + hooks + ledger
make check    # full local gate — green here means CI green
```

<project-specific setup: environments, secrets, run instructions.>

## How this repo works

This repo runs a requirements-first process for agentic development,
tailored from ISO/IEC/IEEE 29148. The short version:

- **Truth is layered and addressable.** The Vision above is the top of the
  spine; it flows down: profiles(+BRDs) → scenarios → PRD (the spec),
  joined by `docs/trace/links.yaml`; decisions are ADRs in
  `docs/decisions/`. Every statement has a permanent identity (uid `15NM7`
  ≡ alias `word.word`); references pin versions; stale pins fail the build.
- **Authored vs derived.** `docs/` and this README are authored; `build/`
  is regenerated by `tools/req` and never edited; `ledger/` is
  machine-written memory, audited against the diff.
- **Three gate rings, one verdict.** Pre-commit hook, optional no-mistakes
  push gate, and CI all run the same checks.

Document flowdown — everything converges on this README, and only the
README branches:

    agents ─→ CLAUDE.md (AGENTS.md / codex.md are shims) ─┐
    humans ───────────────────────────────────────────────┤
                                                          ▼
                 README.md — THE ROOT (the vision lives here)
                                 │
                 ┌───────────────┴────────────────┐
                 ▼                                ▼
             PRODUCT                           PROCESS
     docs/profiles (+ docs/brd)        docs/process.md (NORMATIVE:
       → docs/scenarios                    core + full reference)
         → docs/prd                        → docs/process/* modules
     ⇐ docs/trace/links.yaml ⇒             → tools/req
     architecture + docs/decisions
       → code & tests

On any conflict the process doc wins.
